<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="STFU.UI.SettingsWindow"
        Width="900"
        Height="620"
        MinWidth="760"
        MinHeight="560"
        CanResize="True"
        WindowStartupLocation="CenterOwner"
        Title="Settings"
        Background="{DynamicResource StfuBgBrush}">
  <Border Padding="22">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
      <StackPanel Spacing="20">
        <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
          <TextBlock Grid.Column="0"
                     Text="Settings"
                     FontSize="24"
                     FontWeight="SemiBold"
                     VerticalAlignment="Center" />
          <Button Grid.Column="1"
                  Content="Apply"
                  MinWidth="88"
                  Click="OnApplyClicked" />
          <Button Grid.Column="2"
                  Content="Close"
                  MinWidth="88"
                  Click="OnCloseClicked" />
        </Grid>

        <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="16" RowSpacing="16">
          <Border Grid.Row="0"
                  Grid.Column="0"
                  Padding="14"
                  Background="{DynamicResource StfuSurfaceBrush}"
                  BorderBrush="{DynamicResource StfuLineSoftBrush}"
                  BorderThickness="1">
            <StackPanel Spacing="10">
              <TextBlock Text="Backend" FontWeight="SemiBold" />
              <ComboBox x:Name="BackendCombo"
                        SelectionChanged="OnBackendSelectionChanged">
                <ComboBoxItem Content="Auto" />
                <ComboBoxItem Content="Full CPU" />
                <ComboBoxItem Content="CPU + GPU" />
              </ComboBox>
              <TextBlock Text="{Binding GpuSettingsDisabledReason}"
                         Foreground="{DynamicResource StfuMutedBrush}"
                         TextWrapping="Wrap" />
              <TextBlock Text="{Binding RequestedSummary}"
                         Foreground="{DynamicResource StfuMutedBrush}"
                         TextWrapping="Wrap" />
            </StackPanel>
          </Border>

          <Border Grid.Row="0"
                  Grid.Column="1"
                  Padding="14"
                  Background="{DynamicResource StfuSurfaceBrush}"
                  BorderBrush="{DynamicResource StfuLineSoftBrush}"
                  BorderThickness="1">
            <StackPanel Spacing="10">
              <TextBlock Text="Presentation" FontWeight="SemiBold" />
              <ComboBox x:Name="PresentationCombo"
                        IsEnabled="{Binding CanConfigurePresentation}"
                        SelectionChanged="OnPresentationSelectionChanged">
                <ComboBoxItem Content="Auto" />
                <ComboBoxItem Content="Direct GPU" />
                <ComboBoxItem Content="GPU Readback" />
              </ComboBox>
              <TextBlock Text="{Binding GpuSettingsDisabledReason}"
                         Foreground="{DynamicResource StfuMutedBrush}"
                         TextWrapping="Wrap" />
              <TextBlock Text="This is the requested mode. Runtime Info below shows what is actually active."
                         Foreground="{DynamicResource StfuMutedBrush}"
                         TextWrapping="Wrap" />
            </StackPanel>
          </Border>

          <Border Grid.Row="1"
                  Grid.Column="0"
                  Padding="14"
                  Background="{DynamicResource StfuSurfaceBrush}"
                  BorderBrush="{DynamicResource StfuLineSoftBrush}"
                  BorderThickness="1">
            <StackPanel Spacing="10">
              <TextBlock Text="Graphics API" FontWeight="SemiBold" />
              <UniformGrid Columns="2">
                <RadioButton x:Name="ApiAutoButton"
                             GroupName="RendererApi"
                             Content="Auto"
                             IsEnabled="{Binding CanConfigureGpu}"
                             Click="OnApiChecked" />
                <RadioButton x:Name="ApiDirectX11Button"
                             GroupName="RendererApi"
                             Content="DirectX 11"
                             IsEnabled="{Binding CanConfigureGpu}"
                             Click="OnApiChecked" />
                <RadioButton Content="Vulkan"
                             IsEnabled="False" />
                <RadioButton Content="OpenGL"
                             IsEnabled="False" />
                <RadioButton Content="Direct3D 12"
                             IsEnabled="False" />
              </UniformGrid>
            </StackPanel>
          </Border>

          <Border Grid.Row="1"
                  Grid.Column="1"
                  Padding="14"
                  Background="{DynamicResource StfuSurfaceBrush}"
                  BorderBrush="{DynamicResource StfuLineSoftBrush}"
                  BorderThickness="1">
            <StackPanel Spacing="10">
              <TextBlock Text="Diagnostics" FontWeight="SemiBold" />
              <CheckBox x:Name="ShowHudCheckBox"
                        Content="Show renderer HUD"
                        IsChecked="{Binding ShowRendererHud}" />
              <CheckBox x:Name="GpuTimingsCheckBox"
                        Content="Enable GPU timings"
                        IsEnabled="{Binding CanConfigureGpuTimings}"
                        IsChecked="{Binding EnableGpuTimings}" />
            </StackPanel>
          </Border>

          <Border Grid.Row="2"
                  Grid.Column="0"
                  Padding="14"
                  Background="{DynamicResource StfuSurfaceBrush}"
                  BorderBrush="{DynamicResource StfuLineSoftBrush}"
                  BorderThickness="1">
            <StackPanel Spacing="10">
              <TextBlock Text="Pipeline Strategy" FontWeight="SemiBold" />
              <ComboBox x:Name="PipelineStrategyCombo"
                        SelectionChanged="OnPipelineStrategySelectionChanged">
                <ComboBoxItem Content="Reference Quality" />
                <ComboBoxItem Content="Interactive Performance" />
              </ComboBox>
              <TextBlock x:Name="PipelineStrategyDescriptionText"
                         Text="{Binding PipelineStrategyDescription}"
                         Foreground="{DynamicResource StfuMutedBrush}"
                         TextWrapping="Wrap" />
              <TextBlock x:Name="PipelineStrategyStatusText"
                         Text="{Binding PipelineStrategyStatusNote}"
                         Foreground="{DynamicResource StfuMutedBrush}"
                         TextWrapping="Wrap" />
            </StackPanel>
          </Border>

          <Border Grid.Row="2"
                  Grid.Column="1"
                  Padding="14"
                  Background="{DynamicResource StfuSurfaceBrush}"
                  BorderBrush="{DynamicResource StfuLineSoftBrush}"
                  BorderThickness="1">
            <StackPanel Spacing="10">
              <TextBlock Text="Parallelism" FontWeight="SemiBold" />
              <Grid ColumnDefinitions="150,*" RowDefinitions="Auto,Auto,Auto,Auto" ColumnSpacing="12" RowSpacing="10">
                <TextBlock Grid.Column="0"
                           Grid.Row="0"
                           Text="Worker budget"
                           VerticalAlignment="Center" />
                <ComboBox Grid.Column="1"
                          Grid.Row="0"
                          x:Name="WorkerBudgetCombo"
                          SelectionChanged="OnWorkerBudgetSelectionChanged">
                  <ComboBoxItem Content="Performance" />
                  <ComboBoxItem Content="Balanced" />
                  <ComboBoxItem Content="Max Performance" />
                  <ComboBoxItem Content="Background Safe" />
                  <ComboBoxItem Content="Single Thread Deterministic" />
                  <ComboBoxItem Content="Benchmark" />
                </ComboBox>
                <TextBlock Grid.Column="0"
                           Grid.Row="1"
                           Text="Max workers"
                           VerticalAlignment="Center" />
                <NumericUpDown Grid.Column="1"
                               Grid.Row="1"
                               Value="{Binding MaxRenderWorkers, Mode=TwoWay}"
                               Minimum="0"
                               Maximum="{Binding ProcessorCount}"
                               Increment="1"
                               FormatString="0" />
                <TextBlock Grid.Column="0"
                           Grid.Row="2"
                           Text="Tile parallelism"
                           VerticalAlignment="Center" />
                <CheckBox Grid.Column="1"
                          Grid.Row="2"
                          IsChecked="{Binding EnableTileParallelism}"
                          VerticalAlignment="Center" />
                <TextBlock Grid.Column="0"
                           Grid.Row="3"
                           Text="Resolved"
                           Foreground="{DynamicResource StfuMutedBrush}"
                           VerticalAlignment="Center" />
                <TextBlock Grid.Column="1"
                           Grid.Row="3"
                           Text="{Binding ParallelismSummary}"
                           Foreground="{DynamicResource StfuMutedBrush}" />
              </Grid>
            </StackPanel>
          </Border>

          <Border Grid.Row="2"
                  Grid.Column="1"
                  Padding="14"
                  Background="{DynamicResource StfuSurfaceBrush}"
                  BorderBrush="{DynamicResource StfuLineSoftBrush}"
                  BorderThickness="1">
            <StackPanel Spacing="10">
              <TextBlock Text="Runtime Info" FontWeight="SemiBold" />
              <Grid ColumnDefinitions="150,*" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto" ColumnSpacing="12" RowSpacing="6">
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Requested mode" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding RequestedSummary}" TextWrapping="Wrap" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Active runtime" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding StatusSummary}" TextWrapping="Wrap" />
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Backend" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding EffectiveBackend}" />
                <TextBlock Grid.Row="3" Grid.Column="0" Text="API" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding EffectiveApi}" />
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Presentation" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding EffectivePresentation}" />
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Surface mode" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SurfaceMode}" />
                <TextBlock Grid.Row="6" Grid.Column="0" Text="Direct available" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding DirectPresenterAvailable}" />
                <TextBlock Grid.Row="7" Grid.Column="0" Text="Direct suppressed" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding DirectSuppressed}" />
                <TextBlock Grid.Row="8" Grid.Column="0" Text="Prefer GPU" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="8" Grid.Column="1" Text="{Binding PreferGpuPresentation}" />
                <TextBlock Grid.Row="9" Grid.Column="0" Text="Require readback" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="9" Grid.Column="1" Text="{Binding RequireGpuReadback}" />
                <TextBlock Grid.Row="10" Grid.Column="0" Text="Allow readback" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="10" Grid.Column="1" Text="{Binding AllowGpuReadback}" />
                <TextBlock Grid.Row="11" Grid.Column="0" Text="Show direct host" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="11" Grid.Column="1" Text="{Binding ShowDirectHost}" />
                <TextBlock Grid.Row="12" Grid.Column="0" Text="Draw bitmap" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="12" Grid.Column="1" Text="{Binding DrawBitmap}" />
                <TextBlock Grid.Row="13" Grid.Column="0" Text="Last output" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="13" Grid.Column="1" Text="{Binding LastOutputKind}" />
                <TextBlock Grid.Row="14" Grid.Column="0" Text="GPU readback ms" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="14" Grid.Column="1" Text="{Binding GpuReadbackMs, StringFormat={}{0:0.00}}" />
                <TextBlock Grid.Row="15" Grid.Column="0" Text="Adapter" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="15" Grid.Column="1" Text="{Binding AdapterName}" TextWrapping="Wrap" />
                <TextBlock Grid.Row="16" Grid.Column="0" Text="Status" Foreground="{DynamicResource StfuMutedBrush}" />
                <TextBlock Grid.Row="16" Grid.Column="1" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
              </Grid>
            </StackPanel>
          </Border>
        </Grid>
      </StackPanel>
    </ScrollViewer>
  </Border>
</Window>
